$('#<%= @report.id %>-off').remove();
$('#<%= @report.id %>').append(
    "<%= j link_to :On, off_path(@report), method: :delete, remote: true, id: "#{@report.id}-on", class: 'badge badge-primary' %>"
);