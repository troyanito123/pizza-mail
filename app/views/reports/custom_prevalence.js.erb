


if (<%= @prevalence == 'custom' %>) {

    $('#custom').html(
        "<%= j collection_check_boxes(:custom, :days, @days, :id, :name, checked: @report.days.map(&:to_param) )%>"
    );
    $('#error-prevalence').show();
}

else {
    $('#custom').empty();
    $('#error-prevalence').hide();
}
