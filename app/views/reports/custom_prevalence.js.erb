if (<%= @prevalence == 'custom' %>) {

    $('#custom').html(
        "<%= j render partial: 'day', collection: @days %>"
    );
    $('#error-prevalence').show();
}

else {
    $('#custom').empty();
    $('#error-prevalence').hide();
}
